name: Build binaries

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    name: ${{ matrix.name }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: windows/amd64
            os: windows-latest
            artifact_name: PythIon-windows-amd64
            artifact_path: build/_pyinstaller_dist/PythIon
            executable_path: build/_pyinstaller_dist/PythIon/PythIon.exe
          - name: windows/arm64
            os: windows-11-arm
            artifact_name: PythIon-windows-arm64
            artifact_path: build/_pyinstaller_dist/PythIon
            executable_path: build/_pyinstaller_dist/PythIon/PythIon.exe
          - name: darwin/arm64
            os: macos-14
            artifact_name: PythIon-darwin-arm64
            artifact_path: build/_pyinstaller_dist/PythIon
            executable_path: build/_pyinstaller_dist/PythIon/PythIon

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version-file: pyproject.toml

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true

      - name: Install project
        run: uv sync --locked --no-dev

      - name: Build (PyInstaller)
        env:
          QT_DEBUG_PLUGINS: "0"
        run: uv run scripts/pyinstaller_build.py --out build --name PythIon

      - name: Verify executable
        shell: bash
        run: test -f "${{ matrix.executable_path }}"

      - name: Minimize uv cache
        run: uv cache prune --ci

      - name: Upload artifact
        uses: actions/upload-artifact@v6
        with:
          name: ${{ matrix.artifact_name }}
          path: ${{ matrix.artifact_path }}
          if-no-files-found: error
          retention-days: 14
